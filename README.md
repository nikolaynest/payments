# This is a PAYMENTS service
### responsible for payments handling and storing sufficient status of payments

### How to run locally
1. Create PostgreSQL Database
2. Login to Postgres locally:

   `psql -U admin -d local -W`
3. To restore locally sdkman after intellij Cache Invalidation: 
`source "$HOME/.sdkman/bin/sdkman-init.sh"`